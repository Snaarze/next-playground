'use client'

const AddCart = () => {
  return (
    <div>

      <button className="btn btn-active btn-success" onClick={() => console.log("Clicked")}>Add to cart</button>
    </div>
  );
};

export default AddCart;
